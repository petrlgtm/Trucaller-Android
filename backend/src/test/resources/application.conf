ktor {
    deployment {
        port = 8080
    }
    application {
        modules = []
    }
}

jwt {
    secret = "test-secret-key-for-unit-tests"
    issuer = "trucaller-backend"
    audience = "trucaller-users"
    realm = "trucaller"
}

mongo {
    uri = "mongodb://localhost:27017"
    database = "trucaller-test"
}
